{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build CMake Application",
            "type": "shell",
            "command": "cmake --build ${workspaceFolder}/build --target InstrumentCluster",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}